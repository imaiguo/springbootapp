<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tech.uniontech.springbootapp.mybatis.mapper.UserMapper">
    <select id="selectUserList" resultType="tech.uniontech.springbootapp.mybatis.User">
        select * from t_user;
    </select>
</mapper>
